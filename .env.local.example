# Kopieren nach .env.local (wird nicht versioniert)
# Lokale Entwicklung spricht gegen das LIVE-Backend, kein lokales PHP/DB-Setup nötig.
VITE_API_BASE_URL=https://www.red-it.org/apps/neighborhood/api
